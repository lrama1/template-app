import { Container, Typography } from '@mui/material';

const Claims = () => (
  <Container>
    <Typography variant="h4">Claims</Typography>
  </Container>
);

export default Claims;

// Repeat similar structure for Plans, Claims, and Contact pages